<section class="jumbotron text-center">
    <div class="container">
        <h1 class="jumbotron-heading">{{__('global.welcome.title')}}</h1>
        <p class="lead text-muted">{{__('global.welcome.description')}}</p>
    </div>
</section>

<section class="jumbotron text-center">
    <div class="container">
       <a href="{{route('survey')}}" class="btn btn-primary">{{__('global.btn.start_survey')}}</a>
    </div>
</section>